<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#jams_navbar_collapse">
        <span  class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">JAMS</a> 
    </div> 
    <div class="collapse navbar-collapse" id="jams_navbar_collapse">
      <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <% if can? :read, Game %>
            <li><%= link_to "Games", games_path %></li>
          <% end %>

          <% if can? :read, Event %>
            <li><%= link_to "Events", events_path %></li>
          <% end %>

          <% if can? :read, Person %>
            <li><%= link_to "People", people_path %></li>
          <% end %>

          <% if current_user.has_role?(:admin) %>
          <li class="dropdown">
            <%= link_to "#", class: "dropdown-toggle", data: {toggle: "dropdown"} do %>
              Admin <b class="caret"></b>
            <% end %>
      
            <ul class="dropdown-menu">
              <% if can? :read, Program %>
                <li><%= link_to "Programs", programs_path %></li>
              <% end %>

              <% if can? :manage, User %>
                <li><%= link_to "Users", users_path %></li>
              <% end %>
            </ul>
          </li>
          <% end %>
        </ul>
      <% end %>

      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
          <li>
          <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
          </li>
        <% else %>
          <li>
          <%= link_to 'Login', new_user_session_path %>
          </li>
        <% end %>
        <% if user_signed_in? %>
          <li>
          <%= link_to 'Edit account', edit_user_registration_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>