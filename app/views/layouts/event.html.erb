
<% content_for :content do %>

  <br>

  <div class="row">
    <div class="col s12">
      <ul class="tabs">
        <li class="tab col s2">
        <%= link_to "Judges", event_judges_path(@event), class: active_tab?("events"), target: "_self" %>
        </li>
        <li class="tab col s2">
        <%= link_to "Shirts", event_shirts_path(@event), class: active_tab?("shirts"), target: "_self" %>
        </li>
        <li class="tab col s2">
        <%= link_to "Tools", event_email_builder_path(@event), class: active_tab?("tools"), target: "_self" %>
        </li>
        <li class="tab col s2">
        <%= link_to "Roster", event_roster_path(@event), class: active_tab?("roster"), target: "_self" %>
        </li>
        <li class="tab col s2">
        <%= link_to "Namecards", event_name_cards_path(@event), class: active_tab?("namecards"), target: "_self" %>
        </li>
      </ul>
    </div>
  </div>

  <%= render 'layouts/messages' %>
  <%= yield %>

<% end %>

<%= render template: "layouts/management" %>
