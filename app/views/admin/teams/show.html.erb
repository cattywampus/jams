<nav>
<div class="nav-wrapper">
  <div class="row">
    <div class="col s12">
      <%= link_to "Teams", admin_teams_path, class: "breadcrumb" %>
      <%= link_to @team.name, [:admin, @team], class: "breadcrum" %>
    </div>
  </div>
</div>
</nav>

<div class="card">
  <div class="card-image">
    <%= image_tag @team.logo, alt: @team.name %>
  </div>
  <div class="card-content">
    <span class="card-title"><%= @team.name %></span
  </div>
</div>

